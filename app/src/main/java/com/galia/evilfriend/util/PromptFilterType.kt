package com.galia.evilfriend.util

enum class PromptFilterType {
    /**
     * Filters only the active (not completed yet) prompts.
     */
    ACTIVE_PROMPT,

    /**
     * Filters only the archive prompts.
     */
    ARCHIVE_PROMPT
}